<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Invoice | RMHUB</title>
    <link rel="icon" type="image/x-icon" href="/images/NavImg.jpg">

    <!-- Bootstrap 5 (invoice only, isolated) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <style>
        :root {
            --rm-primary: #3cf696;
            --rm-primary-2: #22d67f;
            --rm-ink: #0f172a;
            --rm-muted: rgba(15, 23, 42, 0.65);
            --rm-border: rgba(15, 23, 42, 0.12);
            --rm-shadow: 0 20px 45px rgba(2, 6, 23, 0.12);
            --rm-radius: 22px;
        }

        body {
            background:
                radial-gradient(1000px 500px at 10% 5%, rgba(60, 246, 150, 0.25), transparent 55%),
                #f8f9fa;
            color: var(--rm-ink);
            font-family: Arial, sans-serif;
        }

        .invoice-wrap {
            max-width: 960px;
            margin: 40px auto;
        }

        .glass {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(255, 255, 255, 0.6);
            border-radius: var(--rm-radius);
            box-shadow: var(--rm-shadow);
            overflow: hidden;
        }

        .inv-head {
            padding: 22px 26px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid var(--rm-border);
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 14px;
        }

        .brand img {
            width: 56px;
            height: 56px;
            border-radius: 14px;
            object-fit: contain;
            background: #fff;
            padding: 6px;
            border: 1px solid var(--rm-border);
        }

        .brand h4 {
            margin: 0;
            font-weight: 900;
            letter-spacing: 0.4px;
        }

        .status-badge {
            padding: 8px 14px;
            border-radius: 999px;
            font-weight: 900;
            background: linear-gradient(135deg, var(--rm-primary), var(--rm-primary-2));
            color: #0f172a;
            white-space: nowrap;
        }

        .inv-body {
            padding: 26px;
        }

        .inv-meta {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 26px;
        }

        .meta-box {
            background: rgba(255, 255, 255, 0.8);
            border: 1px solid var(--rm-border);
            border-radius: 16px;
            padding: 16px;
        }

        .meta-box h6 {
            margin-bottom: 8px;
            font-weight: 900;
        }

        .meta-box p {
            margin: 0;
            font-size: 14px;
            font-weight: 700;
            color: var(--rm-muted);
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 10px;
            margin-top: 10px;
        }

        thead th {
            background: linear-gradient(135deg, var(--rm-primary), var(--rm-primary-2));
            color: #0f172a;
            font-weight: 900;
            padding: 14px;
            border: none;
            white-space: nowrap;
        }

        tbody td {
            background: #fff;
            border-top: 1px solid var(--rm-border);
            border-bottom: 1px solid var(--rm-border);
            padding: 14px;
            vertical-align: middle;
            font-weight: 700;
        }

        tbody td:first-child {
            border-left: 1px solid var(--rm-border);
            border-radius: 14px 0 0 14px;
            font-weight: 900;
        }

        tbody td:last-child {
            border-right: 1px solid var(--rm-border);
            border-radius: 0 14px 14px 0;
            text-align: right;
        }

        .prod-name {
            font-weight: 900;
            margin-bottom: 4px;
        }

        .prod-desc {
            font-size: 12px;
            color: var(--rm-muted);
        }

        .summary {
            max-width: 420px;
            margin-left: auto;
            margin-top: 20px;
        }

        .sum-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 14px;
            border-radius: 14px;
            border: 1px solid var(--rm-border);
            background: rgba(255, 255, 255, 0.85);
            margin-bottom: 10px;
            font-weight: 900;
        }

        .sum-total {
            background: rgba(60, 246, 150, 0.22);
            border-color: rgba(60, 246, 150, 0.35);
            font-size: 16px;
        }

        .actions {
            margin-top: 20px;
            display: flex;
            justify-content: flex-end;
            gap: 10px;
        }

        .btn-rm {
            border: none;
            border-radius: 14px;
            padding: 10px 18px;
            font-weight: 900;
            background: linear-gradient(135deg, var(--rm-primary), var(--rm-primary-2));
            color: #0f172a;
        }

        @media print {
            body {
                background: #fff;
            }

            .actions {
                display: none;
            }

            .glass {
                box-shadow: none;
                border: 1px solid #ccc;
            }

            @page {
                size: A4;
                margin: 8mm;
            }
        }
    </style>
</head>

<body>

    <div class="invoice-wrap">
        <div class="glass">

            <!-- HEADER -->
            <div class="inv-head">
                <div class="brand">
                    <img src="/images/NavImg.jpg" alt="RMHUB">
                    <h4>RMHUB</h4>
                </div>
                <span class="status-badge"><%- order.orderStatus %></span>
            </div>

            <!-- BODY -->
            <div class="inv-body">

                <!-- META -->
                <div class="inv-meta">
                    <div class="meta-box">
                        <h6>Billed To</h6>
                        <p><%- order.shippingAddress %></p>
                    </div>
                    <div class="meta-box text-end">
                        <p><strong>Invoice #</strong>
                            <%= invoiceId %>
                        </p>
                        <p><strong>Order #</strong>
                            <%= order._id.toString().slice(-7).toUpperCase() %>
                        </p>
                        <p><strong>Date</strong>
                            <%= new Date().toDateString() %>
                        </p>
                    </div>
                </div>

                <!-- TABLE -->
                <table>
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Item</th>
                            <th>Price</th>
                            <th>Qty</th>
                            <th>Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% order.orderItems.forEach((item, i)=>{ %>
                            <tr>
                                <td>
                                    <%= i+1 %>
                                </td>
                                <td>
                                    <div class="prod-name"><%- item.product_name %></div>
                                    <div class="prod-desc"><%- item.description %></div>
                                </td>
                                <td>₹<%= Number(item.price).toLocaleString('en-IN') %>
                                </td>
                                <td>
                                    <%= item.quantity %>
                                </td>
                                <td>₹<%= (item.price * item.quantity).toFixed(2) %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>

                <!-- SUMMARY -->
                <div class="summary">
                    <div class="sum-row">
                        <span>Subtotal</span>
                        <span>₹<%= order.orderItems.reduce((a,i)=>a+i.price*i.quantity,0).toFixed(2) %></span>
                    </div>

                    <div class="sum-row">
                        <span>Discount</span>
                        <span>-₹<%= order.discountValue ? order.discountValue.toFixed(2) : '0.00' %></span>
                    </div>

                    <div class="sum-row">
                        <span>Shipping</span>
                        <span>Free</span>
                    </div>

                    <div class="sum-row sum-total">
                        <span>Total</span>
                        <span>₹<%= Number(order.totalPrice).toLocaleString('en-IN') %></span>
                    </div>
                </div>

                <!-- THANK YOU -->
                <div style="
  margin-top:22px;
  padding:16px;
  border-radius:16px;
  background: rgba(60,246,150,0.18);
  border: 1px solid rgba(60,246,150,0.30);
">
                    <div
                        style="display:flex; gap:12px; align-items:flex-start; justify-content:space-between; flex-wrap:wrap;">
                        <div>
                            <div style="font-weight:900; font-size:15px; margin-bottom:4px;">
                                Thank you for shopping with RMHUB.
                            </div>
                            <div style="font-weight:700; color: rgba(15,23,42,0.72); font-size:13px;">
                                We appreciate your trust. Visit again for the latest phones, wearables, and tablets.
                            </div>
                        </div>
                        <div style="font-weight:900; color: rgba(15,23,42,0.85); font-size:13px; white-space:nowrap;">
                            <i class="fas fa-shopping-bag"></i> Come again
                        </div>
                    </div>
                </div>

                <!-- ACTIONS -->
                <div class="actions">
                    <button class="btn-rm" onclick="window.print()">
                        <i class="fas fa-print"></i> Print Invoice
                    </button>
                </div>

            </div>
        </div>
    </div>

</body>

</html>