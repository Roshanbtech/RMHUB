<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RMHUB - Technology Simplified</title>
  <link rel="icon" type="image/x-icon" href="/images/NavImg.jpg">

  <!-- Bootstrap CSS -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

  <!-- Your site CSS (footer classes + shared styling) -->
  <link rel="stylesheet" href="/css/style.css">

  <!-- Font Awesome (nav already includes it, but safe for standalone static) -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" rel="stylesheet">

  <style>
    :root{
      --rm-primary:#3cf696;
      --rm-primary-2:#22d67f;
      --rm-ink:#0f172a;
      --rm-muted: rgba(15, 23, 42, 0.65);
      --rm-border: rgba(15, 23, 42, 0.10);
      --rm-shadow: 0 18px 40px rgba(2, 6, 23, 0.12);
      --rm-radius: 22px;
    }

    body{
      font-family: Arial, sans-serif;
      color: var(--rm-ink);
      background:
        radial-gradient(1100px 600px at 15% 10%, rgba(60, 246, 150, 0.22), transparent 55%),
        radial-gradient(900px 500px at 90% 25%, rgba(60, 246, 150, 0.14), transparent 55%),
        #f8f9fa;
    }

    /* Hero / Carousel polish */
    .hero-wrap{ margin-top: 8px; }
    .carousel{
      border-radius: var(--rm-radius);
      overflow: hidden;
      box-shadow: var(--rm-shadow);
      border: 1px solid rgba(255,255,255,0.65);
      background: rgba(255,255,255,0.65);
    }
    .carousel-item img{
      width:100%;
      height: 420px;
      object-fit: cover;
    }
    @media (max-width: 767.98px){
      .carousel-item img{ height: 260px; }
    }

    /* Section heading chip */
    .title-chip{
      display:inline-flex;
      align-items:center;
      gap:10px;
      padding: 10px 14px;
      border-radius: 999px;
      background: rgba(60,246,150,0.18);
      border: 1px solid rgba(60,246,150,0.28);
      font-weight: 900;
      color: rgba(15,23,42,0.90);
      box-shadow: 0 12px 22px rgba(2,6,23,0.06);
    }

    /* Featured tiles */
    .feat-tile{
      position: relative;
      border-radius: var(--rm-radius);
      overflow:hidden;
      box-shadow: var(--rm-shadow);
      border: 1px solid rgba(255,255,255,0.65);
      background: rgba(255,255,255,0.70);
      min-height: 320px;
      transition: transform 160ms ease, box-shadow 160ms ease;
    }
    .feat-tile:hover{
      transform: translateY(-2px);
      box-shadow: 0 22px 44px rgba(2, 6, 23, 0.14);
    }
    .feat-tile img{
      width:100%;
      height: 320px;
      object-fit: cover;
      transition: transform 220ms ease;
    }
    .feat-tile:hover img{ transform: scale(1.05); }

    .feat-overlay{
      position:absolute;
      left:0; right:0; bottom:0;
      padding: 16px;
      background: linear-gradient(180deg, rgba(2,6,23,0.0), rgba(2,6,23,0.70));
      color:#fff;
    }
    .feat-overlay h3{ margin:0; font-weight: 900; }
    .feat-overlay p{ margin:6px 0 12px 0; font-weight: 700; opacity: 0.95; }

    .btn-rm{
      border:none;
      border-radius: 14px;
      font-weight: 900;
      padding: 11px 16px;
      color: rgba(15,23,42,0.95);
      background: linear-gradient(135deg, var(--rm-primary), var(--rm-primary-2));
      box-shadow: 0 18px 30px rgba(34,214,127,0.18);
      transition: transform 160ms ease, filter 160ms ease;
      display:inline-flex;
      align-items:center;
      gap: 10px;
      white-space: nowrap;
    }
    .btn-rm:hover{ transform: translateY(-1px); filter: brightness(0.98); color: rgba(15,23,42,0.95); text-decoration:none; }

    /* Category cards */
    .cat-card{
      border-radius: var(--rm-radius);
      background: rgba(255,255,255,0.86);
      border: 1px solid rgba(15,23,42,0.08);
      box-shadow: 0 14px 28px rgba(2,6,23,0.08);
      padding: 18px;
      transition: transform 160ms ease, box-shadow 160ms ease, border-color 160ms ease;
      height: 100%;
    }
    .cat-card:hover{
      transform: translateY(-2px);
      box-shadow: 0 22px 44px rgba(2,6,23,0.12);
      border-color: rgba(60,246,150,0.30);
    }
    .cat-icon{
      width: 140px; height: 140px;
      border-radius: 999px;
      border: 2px solid rgba(60,246,150,0.55);
      background: rgba(255,255,255,0.85);
      padding: 10px;
      object-fit: cover;
    }
    .cat-card h4{ margin-top: 12px; font-weight: 900; }
    .cat-card p{ margin: 6px 0 0 0; color: var(--rm-muted); font-weight: 700; }

    /* Secondary carousel */
    .mini-carousel .carousel-item img{
      height: 360px;
      object-fit: cover;
    }
    @media (max-width: 767.98px){
      .mini-carousel .carousel-item img{ height: 240px; }
    }
  </style>
</head>

<body>
  <!-- Use the same nav as main implementation -->
  <%- include("nav") %>

  <div class="container">

    <!-- HERO Carousel -->
    <div class="hero-wrap">
      <div id="carouselHero" class="carousel slide" data-ride="carousel" data-interval="4500">
        <ol class="carousel-indicators">
          <li data-target="#carouselHero" data-slide-to="0" class="active"></li>
          <li data-target="#carouselHero" data-slide-to="1"></li>
          <li data-target="#carouselHero" data-slide-to="2"></li>
        </ol>

        <div class="carousel-inner">
          <div class="carousel-item active">
            <img src="/images/Slide1.png" alt="RMHUB Slide 1">
          </div>
          <div class="carousel-item">
            <img src="/images/Slide2.webp" alt="RMHUB Slide 2">
          </div>
          <div class="carousel-item">
            <img src="/images/Slide3.jpg" alt="RMHUB Slide 3">
          </div>
        </div>

        <a class="carousel-control-prev" href="#carouselHero" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselHero" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
    </div>

    <!-- Featured -->
    <div class="mt-5 d-flex align-items-center justify-content-between flex-wrap" style="gap:10px;">
      <div class="title-chip"><i class="fas fa-star"></i> Featured Picks</div>
      <a class="btn-rm" href="/signup"><i class="fas fa-bolt"></i> Explore Collection</a>
    </div>

    <div class="row mt-3">
      <div class="col-lg-6 mb-4">
        <div class="feat-tile">
          <img src="/images/below1.png" alt="Featured 1">
          <div class="feat-overlay">
            <h3>HONOR 90 5G</h3>
            <p>Share Your Vibe</p>
            <a class="btn-rm" href="/signup"><i class="fas fa-shopping-bag"></i> Shop Now</a>
          </div>
        </div>
      </div>

      <div class="col-lg-6 mb-4">
        <div class="feat-tile">
          <img src="/images/below2.jpg" alt="Featured 2">
          <div class="feat-overlay text-right">
            <h3>realme C53</h3>
            <p>108MP Camera Under 10K</p>
            <a class="btn-rm" href="/signup"><i class="fas fa-shopping-bag"></i> Shop Now</a>
          </div>
        </div>
      </div>
    </div>

    <!-- Categories -->
    <div class="mt-4">
      <div class="title-chip"><i class="fas fa-th-large"></i> Shop by Category</div>

      <div class="row mt-3">
        <div class="col-md-4 mb-3">
          <a href="/signup" style="text-decoration:none;">
            <div class="cat-card text-center">
              <img class="cat-icon" src="/images/phone.gif" alt="Phones">
              <h4>Phones</h4>
              <p>Latest Android phones with premium performance.</p>
            </div>
          </a>
        </div>

        <div class="col-md-4 mb-3">
          <a href="/signup" style="text-decoration:none;">
            <div class="cat-card text-center">
              <img class="cat-icon" src="/images/watches.gif" alt="Watches">
              <h4>Watches</h4>
              <p>Smart wearables that fit your lifestyle.</p>
            </div>
          </a>
        </div>

        <div class="col-md-4 mb-3">
          <a href="/signup" style="text-decoration:none;">
            <div class="cat-card text-center">
              <img class="cat-icon" src="/images/tablet.gif" alt="Tablets">
              <h4>Tablets</h4>
              <p>Work, study, and play — bigger and better.</p>
            </div>
          </a>
        </div>
      </div>
    </div>

    <!-- Secondary Carousel -->
    <div class="mt-5">
      <div class="title-chip"><i class="fas fa-fire"></i> Trending Now</div>

      <div id="carouselSecondary" class="carousel slide mini-carousel mt-3" data-ride="carousel" data-interval="4500">
        <ol class="carousel-indicators">
          <li data-target="#carouselSecondary" data-slide-to="0" class="active"></li>
          <li data-target="#carouselSecondary" data-slide-to="1"></li>
          <li data-target="#carouselSecondary" data-slide-to="2"></li>
        </ol>

        <div class="carousel-inner">
          <div class="carousel-item active">
            <img class="d-block w-100" src="/images/mobc.png" alt="Phones Banner">
          </div>
          <div class="carousel-item">
            <img class="d-block w-100" src="/images/watc.png" alt="Watches Banner">
          </div>
          <div class="carousel-item">
            <img class="d-block w-100" src="/images/tabc.jpg" alt="Tablets Banner">
          </div>
        </div>

        <a class="carousel-control-prev" href="#carouselSecondary" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselSecondary" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
    </div>

  </div>

  <!-- Footer: include ONLY from shared footer file -->
  <%- include("footer") %>

  <!-- JS -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
</body>
</html>
